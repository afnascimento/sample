<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="@dimen/busca_height">

    <androidx.appcompat.widget.AppCompatEditText
            android:background="@drawable/busca_edit_text_bg"
            android:id="@+id/editTextBusca"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:paddingStart="16dp"
            android:paddingEnd="8dp"
            android:inputType="text"
            android:imeOptions="actionDone"
            android:text=""
            android:hint="@string/buscar_por_detalhe"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/btnLupa"/>

    <include layout="@layout/julia_status_detalhe_busca_bt_lupa"
             android:id="@+id/btnLupa"
             android:layout_width="60dp"
             android:layout_height="0dp"
             app:layout_constraintEnd_toEndOf="parent"
             app:layout_constraintTop_toTopOf="parent"
             app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>